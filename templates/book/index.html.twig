{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}
{% block body %}

      <section class="infos mt-5">
    <h2>Mon profil</h2>
    <div class="row">
      <div class="col-md-6 img">
        <img src="{{ asset('/img/marsupilami.png') }}" width="200" height="200"  alt="profil marsupilami" class="img-rounded">
      </div>
      <div class="col-md-6 details">
        <blockquote>
          <h5>Thami</h5>
          <small><cite title="Source Title">zfmlkhsdf mzkdhf mdlkhf azùropasd iarn
          <i class="icon-map-marker"></i></cite></small>
        </blockquote>
        <p>
          thami.sadouk@gmail.com<br>
        </p>
        <a class="btn btn-warning" href="#" role="button" >Modifier profil</a>
      </div>
    </div>
  </section>

  <section class="addressbook mt-5">
    <h2>Mon Carnet d'Adresses</h2>

    <div class="row">
      <table id="contacts" class="table table-hover">
        <thead>
          <tr>
            <th scope="col">Nom</th>
            <th scope="col">Email</th>
            <th scope="col">description</th>
            <th scope="col">Date d'ajout</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>  
        <tbody>
          {% for contact in contacts %}
            <tr>
              <td>{{ contact.username }}</td>
              <td>{{ contact.email }}</td>
              <td>{{ contact.description }}</td>
              <td>{{ contact.createdAt | date('d/m/Y') }}</td>
              <td>
                <div class="d-flex justify content-center">
                  <a class="btn btn-warning" href="#" role="button" >Modifier</a>
                  <a class="btn btn-danger" href="#" role="button" >supprimer</a>
                </div>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table> 
    </div>
  </section>
{% endblock %}
